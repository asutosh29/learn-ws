services:
  redis:
    image: redis:latest # Using a specific, stable version is recommended
    ports:
      - "127.0.0.1:6379:6379"
    command: ["redis-server", "--appendonly", "yes"] # Optional: Enable AOF persistence
